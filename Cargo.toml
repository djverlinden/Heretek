[workspace]
resolver = "3"
members = ["heretekd", "heretekctl", "config", "container_executor"]

# Wijs naar de heretek-config package in de config directory
[workspace.dependencies]
heretek-config = { path = "config" }
